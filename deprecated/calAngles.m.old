function calAngles(D)

const = getConstants();

MphysActual = [1 2 4 8 16 20 25 40] * const.scrScaling;

angles = atand(MphysActual/D);

disp('angles (degrees):');

for i=1:length(angles)
    fprintf('%1.1f\n', angles(i))
end

end